services:
  bot:
    image: storyspin-bot
    build: .
    volumes:
      - './models:/models:ro'
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
