services:
  bot:
    image: storyspin-bot
    build: .
    volumes:
      - './models:/usr/share/models:ro'
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
